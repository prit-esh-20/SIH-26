# API base URL for the CyberTwin backend (FastAPI).
VITE_API_BASE_URL=http://localhost:8000/api

# true  → services resolve from the deterministic mock layer (works without a backend).
# false → services call the backend; API errors surface in the UI with retry.
VITE_USE_MOCK=true
